# Python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.venv/
venv/
build/
dist/

# RL training artifacts (regeneratable via training/train.py; large
# binaries, many checkpoints during experimentation -- commit a specific
# model deliberately if one is ever chosen for the submission)
training/models/

# OS
.DS_Store

# Agent folders (may hold credentials/tokens)
.claude/

# Downloaded Kaggle episode replays (large; regeneratable from the
# EpisodeService API -- see research.md's replay-analysis section).
replays/
submissions/_dev/
